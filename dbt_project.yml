name: mindsphere_temporal_twin
version: 1.0.0
config-version: 2

profile: mindsphere_temporal_twin

model-paths:
  - models
test-paths:
  - tests
seed-paths:
  - seeds
analysis-paths:
  - analyses
macro-paths:
  - macros
snapshot-paths:
  - snapshots

models:
  mindsphere_temporal_twin:
    +database: MINDSPHERE_TWIN_DB
    +schema: ANALYTICS
    staging:
      +materialized: view
    marts:
      +materialized: table

tests:
  +store_failures: true
